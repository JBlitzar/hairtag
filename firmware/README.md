# Firmware

This was one of the hardest installation processes I've ever done, probably superseded in difficulty only to that one time where I rooted a rabbit r1 without any x86 machines.

This will likely only work on a mac, because apple is apple.

For the sake of reproducibility, here's what to do:

1. You're going to need to generate your keys. This leaches off of your apple account.

// insert instructions from https://github.com/biemster/FindMy, run a docker container
// to generate, run the thing, when it asks for 2fa code, log in to icloud, click can't access this device, click send text and then copy that text message code into terminal
// generate the keys
// flash the firmware like the esp32c3 readme says
// run the thing again, should give reports
